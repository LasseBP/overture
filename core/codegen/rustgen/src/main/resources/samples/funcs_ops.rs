use codegen_runtime::*;

/* types */
#[derive(PartialEq, Eq, Clone, Hash, Debug)]
pub struct funcs_ops;
impl funcs_ops {
/* operations */
				pub fn fiveop(&mut self, i: i64) -> i64 {
	return 2 + 3;
} 
		 pub fn new() -> funcs_ops {
			let  instance: funcs_ops = 	funcs_ops::default();
			return instance;
	
} 
		
/* functions */
		pub fn five(i: i64) -> i64 {
	2 + 3
} 
	}

impl Default for funcs_ops {
	fn default() -> funcs_ops {
				
		funcs_ops
			}
}
	

	
