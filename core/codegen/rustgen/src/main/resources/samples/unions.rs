use codegen_runtime::*;

/* types */
pub type U = Object;

 type optional = Option<u64>;

 #[derive(PartialEq, Eq, Clone, Hash, Debug)]
 struct kek {
		pub val: i64,
	}
impl_record! { kek: val as i64 }

#[derive(PartialEq, Eq, Clone, Hash, Debug)]
pub struct unions ;
impl unions {
/* operations */
		pub fn new() -> unions {
			let  instance: unions = unions::default();
			return instance;
	
} 
	
		
/* functions */
		pub fn f1() -> U {
	quotes::T
} 
		pub fn f2() -> Option<u64> {
	123
} 
	}

impl Default for unions {
	fn default() -> unions {
				
		unions
			}
}
	

	
