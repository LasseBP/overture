use codegen_runtime::*;

/* types */
#[derive(PartialEq, Eq, Clone, Hash, Debug)]
pub struct numerics;
impl numerics {
/* operations */
		 pub fn new() -> numerics {
			let  instance: numerics = 	numerics::default();
			return instance;
	
} 
		
/* functions */
		pub fn Run() -> i64 {
	2 + 3
} 
	}

impl Default for numerics {
	fn default() -> numerics {
				
		numerics
			}
}
	

	
