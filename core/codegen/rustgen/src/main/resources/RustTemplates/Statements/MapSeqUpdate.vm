#set( $col = $RustFormat.format($node.getCol()) )
#set( $index = $RustFormat.format($node.getIndex()) )
#set( $value = $RustFormat.format($node.getValue()) )
##
Utils.mapSeqUpdate(${col},${index},${value});