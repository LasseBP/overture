##Example: super.op(1, 1)
##
#set( $super = "super")
##
#set( $return = "")
#if (!$RustFormat.isVoidType($node.getType()))
	#set( $return = "return ")
#end
##
#set( $name = "$node.getName()")
##
${return}${super}.${name}($RustFormat.formatArgs($node.getArgs()));