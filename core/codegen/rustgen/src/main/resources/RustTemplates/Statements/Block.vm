#if ($Util.isScoped($node))
{
#end
##
## Local definitions:
##
	#foreach( $dec in $node.getLocalDefs() )
	$RustFormat.format($dec)
	#end
##
## Statements:
##
	#foreach( $stm in $node.getStatements() )
	$RustFormat.format($stm)
	#end
##
#if ($Util.isScoped($node))
}
#end