#set( $args = $RustFormat.formatList($node.getArgs()) )
#set( $field = $node.getFieldName() )
#set( $designator = $RustFormat.format($node.getDesignator()) )
##
#set( $return = "")
#if (!$Util.isVoidType($node.getType()))
	#set( $return = "return ")
#end
##
${return}${designator}.${field}(${args});