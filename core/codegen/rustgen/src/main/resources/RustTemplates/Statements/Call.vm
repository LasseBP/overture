##Example: A`foo(1, 1)
##
#set( $class = "" )
##
#if (!$Util.isNull($node.getClassType()))
	#set( $separator = "::")
	#set( $classType = "$RustFormat.format($node.getClassType())")
	#set( $class = "$classType$separator")
#end
##
#set( $return = "")
#if (!$Util.isVoidType($node.getType()))
	#set( $return = "return ")
#end
##
#set( $name = "$node.getName()")
##
${return}$class$name($RustFormat.formatArgs($node.getArgs()));