#set( $type = $RustFormat.formatTemplateParam($node.getType()) )
#set( $index = $node.getField() - 1 )
##
#set( $tuple = $RustFormat.format($node.getTuple()) )
#set( $method = ".get($index)" )
##
#set( $field = "")
#if ($RustFormat.isSeqConversion($node))
	#set( $field = "${tuple}${method}")##
	${field}##
#else
	#set( $field = "((${type}) ${tuple}${method})")##
	#if ($ValueSemantics.cloneMember($node))
		Utils.copy(${field})##
	#else
		${field}##
	#end
#end
##