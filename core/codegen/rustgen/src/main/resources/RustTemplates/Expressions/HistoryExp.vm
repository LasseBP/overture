#set( $counterType = $node.getHistype() )
#set( $innerClass = $RustFormat.format($node.getSentinelType()) )
#set( $opName = $node.getOpsname() )
##
sentinel.${counterType}[(( ${innerClass} )sentinel).${opName}]