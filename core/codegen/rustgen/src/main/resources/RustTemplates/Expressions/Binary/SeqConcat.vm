#if ($RustFormat.isStringType($node.getType()))
	$RustFormat.format($node.getLeft(), true) + $RustFormat.format($node.getRight(), false)
#else
	SeqUtil.conc($RustFormat.format($node.getLeft()), $RustFormat.format($node.getRight()))
#end