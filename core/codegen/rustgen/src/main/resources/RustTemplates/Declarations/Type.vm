#if(!$RustFormat.hasAccessInTemplate(${node.getDecl()}))$node.getAccess()#end $RustFormat.format(${node.getDecl()})

##
#if (!$RustFormat.isNull($node.getInv()))
	$RustFormat.format($node.getInv())
#end